import React from 'react';
import { DogMascot } from './DogMascot';

/** Landing-screen mascot — static image with gentle float. */
export function WelcomeMascot(props) {
  return <DogMascot {...props} />;
}
