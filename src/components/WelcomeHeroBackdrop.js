import React from 'react';

import { CosmicScreenBackdrop } from './CosmicScreenBackdrop';

/** Landing hero: celestial backdrop with glow centered on mascot. */
export function WelcomeHeroBackdrop() {
  return <CosmicScreenBackdrop glowY={0.14} />;
}
