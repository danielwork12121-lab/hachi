/**
 * Kanji glyph styling for PetReading pillars & combo blocks.
 * Mapping and colors: src/utils/pillarElements.js (KANJI_ELEMENT_COLORS).
 * UI chrome uses colors.js / elements.js — not these values.
 */

export { KANJI_ELEMENT_COLORS } from '../utils/pillarElements';
